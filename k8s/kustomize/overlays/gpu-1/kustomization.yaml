apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

nameSuffix: "-1gpu"

commonLabels:
  gpu-count: "1"

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: gpu-workload
      spec:
        template:
          spec:
            containers:
            - name: gpu-container
              resources:
                limits:
                  nvidia.com/gpu: "1"
                requests:
                  nvidia.com/gpu: "1"
                  memory: "8Gi"
                  cpu: "4"
