apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

nameSuffix: "-4gpu"

commonLabels:
  gpu-count: "4"

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: gpu-workload
      spec:
        template:
          spec:
            containers:
            - name: gpu-container
              resources:
                limits:
                  nvidia.com/gpu: "4"
                requests:
                  nvidia.com/gpu: "4"
                  memory: "32Gi"
                  cpu: "16"
